# Required: Your API key for AI commit message generation
# Use OPENAI_API_KEY for OpenAI or GROQ_API_KEY for Groq
OPENAI_API_KEY=your-api-key-here

# Optional: Choose your LLM provider ('openai' or 'groq')
LLM_PROVIDER=openai

# Optional: Default git branch for push (defaults to 'main' if not set)
GIT_DEFAULT_BRANCH=main

# Optional: Customize commit message settings
COMMIT_MESSAGE_TEMPERATURE=0.3

# Optional: Model to use (defaults vary by provider)
# For OpenAI: 'gpt-3.5-turbo', 'gpt-4', etc.
# For Groq: 'llama2-70b-chat', 'mixtral-8x7b-chat', etc.
COMMIT_MESSAGE_MODEL=gpt-3.5-turbo

# Optional: Enable debug mode for detailed logging
DEBUG=pushscripts:* 